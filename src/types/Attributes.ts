import type { Attribute } from "./Attribute"

export interface Attributes {
    choir_types: Attribute[],
    genres: Attribute[],
    places: Attribute[],
    styles: Attribute[],
    regions: Attribute[],
    seasons: Attribute[],
}